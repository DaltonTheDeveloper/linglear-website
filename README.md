# Linglear Website

Static prototype implementing the Linglear outline with 13 themed pages.

## Getting started

Open `index.html` in a browser (or serve the folder with any static file server) to explore the
public pages. Logged-in dashboards are accessible via the standalone HTML files, such as
`dashboard.html`, to showcase the authenticated experience.
